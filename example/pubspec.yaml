name: zairn_example
description: "Zairn SDK example — Dense location trace collector & SDK demo"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  zairn_sdk:
    path: ../
  supabase_flutter: ^2.10.0
  geolocator: ^13.0.0
  intl: ^0.19.0
  path_provider: ^2.1.0
  share_plus: ^10.0.0
  flutter_foreground_task: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
